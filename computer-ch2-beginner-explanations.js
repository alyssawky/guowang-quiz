// 第二章《数据的表示与运算》零基础解析增强。
// 原题、原答案、原解析全部保留；仅在前面增加“零基础补充”和针对薄弱点的逐步推导。
(function () {
  if (window.__computerCh2BeginnerExplanationsInstalled) return;
  window.__computerCh2BeginnerExplanationsInstalled = true;

  const esc = v => String(v == null ? "" : v)
    .replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");

  function baseBlock(q) {
    const topic = String(q.topic || "");
    const stem = String(q.question || "");

    if (/2-1|进位记数制|数据表示基础/.test(topic)) {
      return `<strong>先把“进制”理解清楚：</strong><br>
      进制就是“逢几进一”。十进制逢10进1，二进制逢2进1，八进制逢8进1，十六进制逢16进1。<br>
      一个R进制数从右往左的位权依次是 R⁰、R¹、R²……。例如十进制54=5×10+4；同样地，八进制(54)₈=5×8+4。<br>
      <strong>合法数字范围：</strong>R进制每一位只能出现0～R−1。二进制只有0、1；八进制只有0～7；十六进制是0～9和A～F，其中A=10、B=11、C=12、D=13、E=14、F=15。`;
    }

    if (/2-2|数制转换/.test(topic)) {
      return `<strong>先分清你是在做哪一种转换：</strong><br>
      ① <strong>R进制 → 十进制：</strong>按位权展开，每一位×对应的R的幂，再相加。<br>
      ② <strong>十进制整数 → R进制：</strong>不断“除R取余”，每次拿上一步的<strong>商</strong>继续除，直到商为0；最后只读余数，而且要<strong>从最后一个余数往前倒着读</strong>。<br>
      为什么倒着读？第一次得到的余数代表最右边的R⁰位，也就是最低位；越后面的余数位置越高，所以答案必须反过来写。<br>
      ③ <strong>二进制 ↔ 八/十六进制：</strong>二进制每3位对应1位八进制，每4位对应1位十六进制；分组不足时只在最左边补0。`;
    }

    if (/2-3|2-4|补码|机器数/.test(topic) && !/浮点/.test(topic)) {
      return `<strong>先分清“真值”和“机器码”：</strong><br>
      人写的 +53、−53 叫真值，前面的“+ / −”是数学符号，<strong>不占二进制位</strong>；计算机真正存的是固定长度的0和1，这才是机器码。<br>
      <strong>正数：</strong>原码、反码、补码相同，前面补0到规定字长即可。<br>
      <strong>负数求补码：</strong>先写出对应正数的固定字长二进制 → 每一位0/1全部取反 → 再+1。<br>
      <strong>补码还原负数：</strong>最高位为1时，先判断它是负数；再“取反+1”求绝对值，最后加负号。<br>
      8位补码的范围是 −128～127；8位<strong>无符号</strong>数则是0～255，这两个范围不要混。`;
    }

    if (/浮点|IEEE/.test(topic + stem)) {
      return `<strong>把浮点数先类比成科学计数法：</strong><br>
      十进制会写成 6.25×10ⁿ；二进制浮点数也是“尾数×2的指数”。规格化只是把小数点移动到规定位置，同时用指数补偿，保证数值本身不变。<br>
      小数点每左移1位，相当于原数被除以2，所以外面要乘2¹补回来；左移几位就补几次。IEEE 754则是规定计算机怎样保存浮点数的符号、指数和尾数。`;
    }

    if (/2-5|BCD/.test(topic)) {
      return `<strong>BCD不是“把整个十进制数直接转成二进制”：</strong><br>
      它是把十进制的<strong>每一位数字分别编码</strong>。例如十进制59会分别处理5和9。<br>
      “有权码”表示每一个二进制位都有固定权值，例如8421码四位的权依次是8、4、2、1；某组代码代表多少，就把值为1的位置对应权值相加。`;
    }

    if (/2-6|算术/.test(topic)) {
      return `<strong>算术运算是在“算数值”：</strong><br>
      加、减、乘、除属于算术运算。二进制算术和十进制一样会进位，例如1+1=10₂。<br>
      <strong>进位≠溢出：</strong>进位只是某一位向高位传1；溢出是最终数学结果超过当前固定字长能表示的范围。`;
    }

    if (/2-7|溢出/.test(topic)) {
      return `<strong>先理解“为什么会溢出”：</strong><br>
      计算机给一个数的位数是有限的，例如8位补码只能装 −128～127。数学上可以算出128，但8位补码没有位置能正确表示它，这才叫溢出。<br>
      所以做溢出题先不要急着看二进制：先写出该字长的允许范围，再把每个选项按普通数学算出真值，看有没有越界。`;
    }

    if (/2-8|逻辑/.test(topic)) {
      return `<strong>逻辑运算不是普通加减法，而是“每一位各算各的”：</strong><br>
      AND（与）：1和1才是1；OR（或）：只要有一个1就是1；XOR（异或）：两个不同才是1；NOT（非）：0变1、1变0。<br>
      各位互不影响，所以<strong>逻辑运算没有进位，也没有算术意义上的溢出</strong>。清0常用AND 0；置1常用OR 1。`;
    }

    if (/2-9|ASCII|Unicode|信息编码/.test(topic + stem)) {
      return `<strong>先区分“底层二进制”和“编码规则”：</strong><br>
      计算机最终都存0和1，但“这些0和1代表什么”要靠编码规则解释。ASCII、Unicode、GB2312等都是不同的字符编码规则；它们不是“所有信息唯一的编码”。`;
    }

    if (/2-11|汉字编码|字库|GB2312|GBK|GB18030|BIG5/.test(topic + stem)) {
      return `<strong>把汉字处理看成一条流程：</strong><br>
      用户输入汉字 → 使用<strong>输入码</strong>（如拼音、五笔） → 系统内部保存/处理时使用相应内部编码 → 显示或打印时再根据<strong>字形/字模</strong>把它画出来。<br>
      本章计算题最重要的一条链是：<strong>区位码 → 国标码 → 机内码</strong>。按本课程口径：区位码两个十进制数分别+32（即+20H）得到国标码；国标码每个字节再+80H得到机内码。反方向就分别减80H、减20H。<br>
      注意：H表示十六进制。十六进制A=10、B=11、C=12、D=13、E=14、F=15。`;
    }

    if (/2-12|校验/.test(topic + stem)) {
      return `<strong>先按“功能”区分校验码：</strong><br>
      奇/偶校验：规则简单，主要做基础检错；CRC：把一批数据/数据块整体做检错；海明码：通过多个校验位定位错误，典型考点是可纠正1位错误。<br>
      所以看到“校验”不要立刻把所有校验码混在一起，要看题目强调的是<strong>简单检错、整批数据检错，还是定位纠错</strong>。`;
    }

    return `<strong>零基础读题法：</strong><br>先找题目对象是什么，再找它属于哪条定义或规则；如果有计算，先写出规则，再代数值，不要从答案字母反推。`;
  }

  const focus = {
    "c2-q01": `<strong>这里不要跳步：215₁₀到底怎样变成二进制？</strong><br>
      用“除2取余”完整写：<br>
      215÷2=107……1；107÷2=53……1；53÷2=26……1；26÷2=13……0；13÷2=6……1；6÷2=3……0；3÷2=1……1；1÷2=0……1。<br>
      现在只看余数，得到的先后顺序是 1、1、1、0、1、0、1、1。<br>
      <strong>必须倒着读</strong>：11010111₂。因为第一次余数是最低位2⁰，最后一次余数才是最高位。<br>
      可以反查：128+64+16+4+2+1=215，所以11010111₂正确。`,

    "c2-q03": `<strong>这里不要跳步：不同进制为什么要这样展开？</strong><br>
      十进制54其实就是5×10+4；同理，(54)₈中的左边5在“8¹位”，所以(54)₈=5×8+4=44₁₀。<br>
      (2A)₁₆中A=10，左边2在“16¹位”，所以=2×16+10=32+10=42₁₀。<br>
      A项(1010010)₂从右往左权值是1、2、4、8、16、32、64，因此=64+16+2=82。<br>
      最后比较82、44、44、42，最小是42，所以选D。`,

    "c2-q04": `<strong>这里不要跳步：为什么8位无符号是0～255，而不是−128～127？</strong><br>
      8个二进制位每位只有0/1，因此共有2⁸=256种组合。<br>
      如果是<strong>无符号</strong>，8位全部拿来表示大小：最小00000000=0，最大11111111=128+64+32+16+8+4+2+1=255。<br>
      如果是<strong>8位有符号补码</strong>，最高位参与表示正负，范围才是−128～127。题目明确说“无符号”，所以必须用0～255。`,

    "c2-q05": `<strong>这里不要跳步：R到底是什么？</strong><br>
      R就是“几进制”的那个基数。比如R=10就是十进制，R=2就是二进制。<br>
      最直观地看竖式：11+1要正好变成100，说明最低位的1+1已经“满R进1”。两个1就满，只有R=2。<br>
      如果用公式：在R进制中(11)ᵣ=1×R+1=R+1，(100)ᵣ=1×R²=R²。代入得1+(R+1)=R²，即R²−R−2=0=(R−2)(R+1)。进制基数必须大于1，所以R=2。`,

    "c2-q07": `<strong>这里不要跳步：“+0110101”前面的+号不是一个二进制位。</strong><br>
      它表示真值是正数。0110101本身只有7位，所以题目要求8位时要在最左边补0：00110101。<br>
      正数的原码、反码、补码相同，因此不用取反、也不用+1，答案就是00110101。`,

    "c2-q08": `<strong>这里不要跳步：“−0110101”也不是8位机器码。</strong><br>
      最前面的−号只是告诉你“这个真值是负数”，不占位。先把它的绝对值0110101补成8位：00110101。<br>
      然后求负数补码：00110101逐位取反→11001010；再加1→11001011。<br>
      所以−53的8位补码是11001011。`,

    "c2-q14": `<strong>这里不要跳步：区位码2020为什么最后变成3434H？</strong><br>
      2020表示“区号20、位号20”，这里两个20都是<strong>十进制</strong>。<br>
      国标码规则：区号和位号各加32十进制，所以20+32=52，两边都得到52。<br>
      现在把52₁₀转十六进制：52÷16=3……4；3÷16=0……3。只读余数并倒着读，得到34，所以52₁₀=34H。<br>
      两个字节都是34H，于是国标码=3434H。这里“+20H”与“+32十进制”是同一件事，因为20H=32₁₀。`,

    "c2-q15": `<strong>这里不要跳步：34H+80H为什么等于B4H？</strong><br>
      十六进制仍然是按位算。个位4+0=4；高位3+8=11。十六进制中10=A、11=B，所以高位11写成B。<br>
      因此34H+80H=B4H。两个字节都这样算，所以3434H→B4B4H。`,

    "c2-q16": `<strong>这里不要跳步：D1H−80H和A7H−80H怎样减？</strong><br>
      十六进制字母先换成数值：A=10，D=13。<br>
      D1H−80H：个位1−0=1；高位D−8就是13−8=5，所以得到51H。<br>
      A7H−80H：个位7−0=7；高位A−8就是10−8=2，所以得到27H。<br>
      合起来是5127H。这里不是把整个D1A7一次减80，而是<strong>两个字节分别减80H</strong>。`,

    "c2-q17": `<strong>这里不要跳步：二进制小数点两边的位权不同。</strong><br>
      110.01₂中，小数点左边从右往左是2⁰、2¹、2²；右边从左往右是2⁻¹、2⁻²。<br>
      所以整数部分110₂=1×4+1×2+0×1=6；小数部分.01₂=0×1/2+1×1/4=0.25；合起来=6.25。`,

    "c2-q19": `<strong>这里不要跳步：1101₂为什么等于D₁₆？</strong><br>
      四位二进制的位权从左到右是8、4、2、1。1101表示1×8+1×4+0×2+1×1=13。<br>
      十六进制一位可以表示0～15，其中10=A、11=B、12=C、13=D，所以13写成D。<br>
      因而1101₂=D₁₆。`,

    "c2-q21": `<strong>这里不要跳步：11111110为什么不是254，而是−2？</strong><br>
      如果题目说“无符号数”，11111110确实是254；但题目明确说它是<strong>8位补码</strong>。补码最高位为1代表负数。<br>
      还原绝对值：11111110取反→00000001；再+1→00000010=2，所以真值是−2。<br>
      这就是为什么同一串0/1，在“无符号”和“补码”解释下会代表不同数值。`,

    "c2-q22": `<strong>这里不要跳步：为什么110.1₂变成0.1101₂后要乘2³？</strong><br>
      从110.1移动到0.1101，小数点一共向左移动3位。单看0.1101，它只有原来的1/2³，也就是1/8。<br>
      为了让整个数值保持不变，外面必须再乘2³补回来。3的二进制是11₂；题目指数栏按3位写成011，所以得到2^(011)×0.1101。`,

    "c2-q23": `<strong>这里不要跳步：8位补码范围怎么来的？</strong><br>
      n位补码范围是−2^(n−1)～2^(n−1)−1。8位就是−2⁷～2⁷−1，即−128～127。<br>
      然后逐项按普通数学算：96+32=128（超过127，溢出）；96−32=64；−96−32=−128（刚好在边界内，不溢出）；−96+32=−64。`,

    "c2-q28": `<strong>这里不要跳步：bit和Byte不是一回事。</strong><br>
      24×24点阵一共有576个点，每个点只需要1个二进制位表示亮或灭，所以先得到576 <strong>bit</strong>。<br>
      8 bit=1 Byte，因此576÷8=72 Byte。题目问“字节”，所以必须再除以8，不能直接选576。`,

    "c2-q32": `<strong>这里不要跳步：为什么八进制3位一组、十六进制4位一组？</strong><br>
      因为8=2³，所以1位八进制正好对应3位二进制；16=2⁴，所以1位十六进制正好对应4位二进制。<br>
      (247)₈：2→010，4→100，7→111，拼成010100111。<br>
      转十六进制时从右往左每4位分组，不足的最左边补0：0 1010 0111 → 1010 0111。1010=10=A，0111=7，所以是A7₁₆。`,

    "c2-q33": `<strong>这里不要跳步：“太大存不下”说的是超出当前表示范围。</strong><br>
      不是电脑坏了，也不是算法错了。比如8位补码最大只能表示127，数学结果128虽然本身没有错，但这个8位盒子装不下它，于是发生溢出。负方向同样可能溢出，例如结果小于−128。`,

    "c2-q38": `<strong>这里不要跳步：原码、反码、补码不是“所有位随便取反”。</strong><br>
      对正数，它们形式相同。对负数，从原码到反码时通常保持符号位不变，只把数值位取反；从反码到补码再+1。<br>
      移码和补码的常见关系是：同一字长下，把补码的最高位取反即可得到移码。`,

    "c2-q39": `<strong>这里不要跳步：为什么8位补码负数会比正数多一个？</strong><br>
      8位共有256种状态。补码只给0保留一种编码，因此可以覆盖128个负数（−128～−1）、1个0、127个正数（1～127）。所以不是“正数多一个”，而是负数多一个。`,

    "c2-q41": `<strong>本题要特别看课程口径：</strong><br>
      你上传的原题库明确给出ABC，因此网站按原题库答案保留ABC。更严格的编码术语会区分“国标交换码”和“机内码”，所以B在其他教材里可能出现不同表述。做你这套考试题时按课程/题库口径答ABC。`,

    "c2-q47": `<strong>这里不要跳步：进位和溢出完全不是一个概念。</strong><br>
      <strong>进位：</strong>某一位相加满了向左边送1，例如二进制1+1=10，会产生进位。<br>
      <strong>溢出：</strong>最终结果超出规定字长的可表示范围。算术运算两种情况都可能出现。<br>
      逻辑运算则是对应位独立做AND/OR/XOR/NOT，不会把某一位的结果“传给下一位”，所以没有进位，也没有算术溢出。`,

    "c2-q49": `<strong>这里不要跳步：补码为什么能简化CPU？</strong><br>
      补码把减法A−B统一成A+(−B)的补码加法，这样CPU不需要为“加法”和“减法”准备完全不同的一套核心运算逻辑。<br>
      同时0只有000…0一种表示；n位补码范围是−2^(n−1)～2^(n−1)−1，所以负数比正数多表示一个。`
  };

  const styleId = "computer-ch2-beginner-explanation-style";
  if (!document.getElementById(styleId)) {
    const style = document.createElement("style");
    style.id = styleId;
    style.textContent = `
      .ch2-beginner-base,.ch2-beginner-focus{margin:10px 0 14px;padding:12px 14px;border-radius:12px;line-height:1.75}
      .ch2-beginner-base{background:rgba(59,130,246,.07);border:1px solid rgba(59,130,246,.18)}
      .ch2-beginner-focus{background:rgba(245,158,11,.08);border:1px solid rgba(245,158,11,.22)}
      .ch2-original-explanation-title{margin-top:14px;padding-top:10px;border-top:1px dashed rgba(127,127,127,.32);font-weight:700}
    `;
    document.head.appendChild(style);
  }

  const chapterQuestions = (typeof questions !== "undefined" && Array.isArray(questions))
    ? questions.filter(q => q && q.taskId === "computer-002" && /^c2-q\d+$/i.test(String(q.id || "")))
    : [];

  let enhanced = 0;
  let focused = 0;
  chapterQuestions.forEach(q => {
    if (q.__ch2BeginnerEnhanced) return;
    const original = q.explanation || "";
    const base = baseBlock(q);
    const extra = focus[q.id] || "";
    q.explanation = `<div class="ch2-beginner-base"><strong>零基础补充｜先把这层基础补上</strong><br>${base}</div>` +
      (extra ? `<div class="ch2-beginner-focus"><strong>你的高风险薄弱点｜这里不要跳步骤</strong><br>${extra}</div>` : "") +
      `<div class="ch2-original-explanation-title">原题解析（保留）</div>${original}`;
    q.__ch2BeginnerEnhanced = true;
    q.beginnerExplanationLevel = extra ? "focus" : "foundation";
    enhanced += 1;
    if (extra) focused += 1;
  });

  window.computerCh2BeginnerExplanationReport = {
    enhanced,
    focused,
    expected: 50,
    missing: Math.max(0, 50 - enhanced)
  };
  console.info("第二章零基础解析增强", window.computerCh2BeginnerExplanationReport);
})();
